# ── Server ──
server.port=8080

# ── Logging ──
logging.level.com.example.musicapi=DEBUG
logging.level.org.springframework.web=INFO

# ── CORS (cho phép Android app gọi API) ──
# Đã cấu hình trong CorsConfig.java
